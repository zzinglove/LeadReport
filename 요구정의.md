# FY26 SFDC Lead 웹 레포트 요구정의

## 1. 문서 목적

현재 PowerPoint 레포트의 분석 결과를 웹 레포트로 전환한다. 웹 레포트는 Excel 전체 데이터를 업로드할 때마다 최신 기준으로 다시 계산하며, 주간·월간·누적 및 과거 업로드 시점의 수치를 함께 관리한다.

이 문서는 사용자 요청, 인터뷰 결과, 확정된 의사결정, 미확정 사항과 변경 이력을 누적 기록하는 기준 문서다.

현재 승인된 1차 설계안은 [`docs/superpowers/specs/2026-09-17-fy26-sfdc-lead-report-design.md`](docs/superpowers/specs/2026-09-17-fy26-sfdc-lead-report-design.md)에 기록한다.
승인된 설계의 구현 계획은 [`docs/superpowers/plans/2026-09-17-fy26-sfdc-lead-report-plan.md`](docs/superpowers/plans/2026-09-17-fy26-sfdc-lead-report-plan.md)에 기록한다.

## 2. 원본 파일

- PPT: `FY26 SFDC Lead report 20260909.pptx`
- Excel: `report1789101088340.xls.xlsx`
- PPT 기준일: 2026년 9월 9일
- Excel 구조: 단일 시트, 40개 컬럼, 현재 확인 기준 1,409건
- Excel 업로드 방식: 매번 Salesforce에서 내려받은 전체 데이터

## 3. 확정된 목표

- PPT의 현재 결과와 분석 체계를 중심으로 웹 레포트를 구성한다.
- Excel 업로드 시 최신 데이터 기준으로 숫자와 차트를 다시 계산한다.
- PPT에 있는 주간 관리뿐 아니라 월간 관리도 제공한다.
- 최신 수치뿐 아니라 과거 주차·월차·업로드 시점의 수치를 보존한다.
- 사내 사용자만 접근할 수 있는 비공개 웹앱으로 운영한다.

## 4. 데이터 집계 기준

### 4.1 기간

- 주간 집계: 월요일~일요일
- 기간 구분 필드: `Created Date`
- 월간 집계: `Created Date`의 연·월 기준
- 상태 집계: 업로드 시점에 Excel에 기록된 현재 `Lead Status`
- 상태 변경 이력이 없으므로 과거 상태 변경 과정을 추정하지 않는다.

### 4.2 식별과 최신 데이터

- Lead 식별키: `SFDC Lead Code`
- 업로드 파일은 전체 데이터이므로 단순 행 추가 방식은 사용하지 않는다.
- `SFDC Lead Code`가 비어 있고 Lead 필드가 채워지지 않은 Salesforce 내보내기 푸터 행은 데이터에서 제외한다.
- 유효한 Lead 데이터에는 `SFDC Lead Code`가 있어야 한다.
- 최신 현황은 최신 업로드 스냅샷을 기준으로 계산한다.
- 같은 Lead의 최신 상태와 변경값은 최신 스냅샷에서 반영한다.
- Excel 업로드 전 필수 컬럼과 컬럼명을 검증한다.
- 필수 컬럼이 누락되거나 컬럼명이 변경된 파일은 업로드를 거부한다.
- 검증 실패 시 누락·변경된 컬럼명을 사용자에게 안내한다.
- 검증에 실패한 파일은 스냅샷 저장, 기존 데이터 교체, 집계 갱신을 수행하지 않는다.
- 필수 컬럼은 다음 15개로 확정한다: `Created Date`, `Organization Level 2`, `Organization Level 3`, `Lead Owner: Full Name`, `SFDC Lead Code`, `Lead Source`, `Lead Status`, `Company`, `Full Name`, `Converted Date`, `Converted Opportunity: Opportunity Status`, `Converted Opportunity: Total GI`, `Converted Opportunity: ORS Revenue`, `Main Interest Area`, `Last Modified Date`.
- 필수 검증을 통과한 파일도 즉시 운영 데이터에 반영하지 않는다.
- 관리자에게 업로드 데이터의 요약 및 집계 미리보기를 먼저 보여준다.
- 관리자가 `반영` 버튼을 눌렀을 때만 최신 스냅샷, Current Master, Historical Metrics를 갱신한다.
- 관리자가 반영하지 않은 파일은 운영 리포트와 스냅샷에 반영하지 않는다.

### 4.3 Business 분류

Excel의 `Main Interest Area`는 상품명이며, 집계용 `Business` 필드를 별도로 생성한다.

| Business | Main Interest Area |
|---|---|
| DT | Multi-Function Device, Printer, Supplies & paper |
| GC | Production Press |
| BS | IT Services, Software, Business Solutions, Business Service |
| ETC | Others |

- 상품명 원문은 `Main Interest Area`로 유지한다.
- Business 매핑은 별도 관리 가능한 매핑 테이블로 둔다.
- DB 컬럼명은 Excel 원본 컬럼명과 분리해 관리한다.
- Excel 원본 컬럼명은 업로드 검증과 원본 추적을 위해 매핑 정보에 보존한다.
- DB 내부 컬럼명은 기본적으로 영문 `snake_case`를 제안하되, 최종적으로는 사용자가 확정한 컬럼명을 우선 사용한다.
- DB 컬럼 매핑표는 사용자의 확인을 받으며 Excel 원본 컬럼별로 순차 작성한다.
- 사용자가 승인한 매핑만 최종 스키마와 `Column Mapping`에 반영한다.
- 사용자가 수정하거나 거절한 DB 컬럼명은 임의로 대체하지 않고 다음 인터뷰에서 재확인한다.

### 4.4 확정된 DB 컬럼 매핑

| Excel 원본 컬럼명 | DB 컬럼명 | 상태 |
|---|---|---|
| `Created Date` | `Lead생성날짜` | 사용자 확정 |
| `Organization Level 2` | `부서` | 사용자 확정 |
| `Organization Level 3` | `팀` | 사용자 확정 |
| `Lead Owner: Full Name` | `담당SR` | 사용자 확정 |
| `SFDC Lead Code` | `SFDC_Lead_Code` | 사용자 확정 |
| `Lead Source` | `Lead유입경로` | 사용자 확정 |
| `Lead Status` | `Lead상태` | 사용자 확정 |
| `Disqualified Reasons` | `Lead부적격사유` | 사용자 확정 |
| `Disqualified Comments` | `Lead부적격코멘트` | 사용자 확정 |
| `Company` | `고객명` | 사용자 확정 |
| `Full Name` | `고객담당자명` | 사용자 확정 |
| `Needs` | `고객Needs` | 사용자 확정 |
| `Customer's Comments` | `고객Comment` | 사용자 확정 |
| `Last Activity` | `최근활동날짜` | 사용자 확정 |
| `Converted Date` | `전환날짜` | 사용자 확정 |
| `Converted to Opportunity` | `Opportunity전환여부` | 사용자 확정 |
| `Converted Opportunity: SFDC Project Code` | `Opportunity_Code` | 사용자 확정 |
| `Converted Opportunity: Opportunity Status` | `Opportunity상태` | 사용자 확정 |
| `Converted Opportunity: Stage` | `Opportunity단계` | 사용자 확정 |
| `Converted Opportunity: Last Activity Result Comments by Sales` | `SR활동코멘트` | 사용자 확정 |
| `Converted Opportunity: Total GI` | `Opportunity_GI` | 사용자 확정 |
| `Converted Opportunity: ORS Revenue Currency` | `통화` | 사용자 확정 |
| `Converted Opportunity: ORS Revenue` | `Opportunity_매출` | 사용자 확정 |
| `Converted Opportunity: (Target) Contract Signing Date` | `계약예정일` | 사용자 확정 |
| `State/Province` | `시도` | 사용자 확정 |
| `City` | `시군구` | 사용자 확정 |
| `Remarks` | `Lead비고` | 사용자 확정 |
| `SFDC Site: Name` | `SFDC_사이트명` | 사용자 확정 |
| `Converted Account: Customer: Name` | `전환고객명` | 사용자 확정 |
| `new or existing` | `고객구분` | 사용자 확정 |
| `new or existing(converted account)` | `전환고객구분` | 사용자 확정 |
| `Created By: Full Name` | `Lead생성자` | 사용자 확정 |
| `Converted Account: SFDC Code` | `전환고객_SFDC_Code` | 사용자 확정 |
| `SFDC Site: SFDC Code` | `SFDC_사이트_Code` | 사용자 확정 |
| `Main Interest Area` | `관심제품` | 사용자 확정 |
| `Last Modified By: Full Name` | `최종수정자` | 사용자 확정 |
| `Last Modified Date` | `최종수정날짜` | 사용자 확정 |
| `Source Campaign: Campaign Name` | `캠페인명` | 사용자 확정 |
| `Main Interest Area`에서 파생 | `Business` | DT / GC / BS / ETC, 사용자 확정 |

### 4.5 DB 저장 제외 컬럼

| Excel 원본 컬럼명 | 처리 |
|---|---|
| `Lead Owner: Role: Name` | DB 저장 및 집계에서 제외 |
| `Converted Account: Site: Name` | DB 저장 및 집계에서 제외 |
- 사용자가 지정한 Business 표기는 `BS`로 통일한다.
- 향후 상품군이 추가되면 매핑 테이블을 갱신한다.
- 업로드 시 `Main Interest Area`에 Business 매핑이 없는 상품명이 발견되면 업로드 반영을 중단한다.
- 관리자가 새로운 상품명과 Business를 먼저 매핑한 뒤에만 해당 파일을 반영할 수 있다.

## 5. 데이터 보존 및 이력 관리

### 5.1 권장 저장 구조

- `Raw Snapshot`: 업로드된 Excel 원본 행 전체와 업로드 일시/기준일
- `Current Master`: 최신 스냅샷 기준의 현재 Lead 상태
- `Historical Metrics`: 업로드 시점별 주간·월간·누적 집계 결과
- `Business Mapping`: 상품명과 Business 간 매핑 정보
- `Column Mapping`: Excel 원본 컬럼명과 DB 내부 컬럼명 간 매핑 정보

### 5.2 스냅샷 원칙

- 업로드할 때마다 전체 Excel 데이터를 하나의 불변 스냅샷으로 보관한다.
- 과거 업로드 시점의 집계 결과는 이후 Lead Status 변경으로 덮어쓰지 않는다.
- 업로드가 여러 번 발생한 주에는 해당 주의 가장 최근 업로드를 대표 스냅샷으로 사용한다.
- 업로드가 없는 주는 임의로 보간하지 않고 데이터 없음으로 표시한다.
- 화면에는 데이터 기준일과 마지막 업로드일을 표시한다.
- 사용자는 웹 화면에서 업로드 스냅샷이 존재하는 날짜만 기준일로 선택할 수 있다.
- 스냅샷 기준일은 관리자가 입력하지 않고 Excel을 업로드한 날짜를 자동으로 사용한다.
- 업로드 날짜와 시간은 시스템 기준 시간대인 Asia/Seoul 기준으로 기록한다.
- 기준일 선택기는 실제 스냅샷 날짜 목록만 제공하며, 임의 날짜 입력은 허용하지 않는다.
- 선택한 기준일의 리포트는 해당 날짜의 업로드 스냅샷을 기준으로 표시한다.
- 다른 날짜의 데이터로 대체하거나 기준일과 가장 가까운 날짜를 자동 선택하지 않는다.
- 같은 날짜에 여러 번 업로드하면 해당 날짜의 마지막 업로드만 유효한 스냅샷으로 사용한다.
- 같은 날짜의 이전 업로드는 기준일 선택 대상과 최신 리포트 계산에서 제외한다.
- 최근 2년 이내의 유효 스냅샷은 날짜별로 보관한다.
- 2년이 지난 스냅샷은 각 연·월별로 가장 빠른 날짜의 스냅샷 하나만 보관한다.
- 2년이 지난 스냅샷의 대표 날짜에 업로드가 여러 번 있었다면 해당 날짜의 마지막 업로드를 보관한다.
- 스냅샷 정리 작업은 유효한 대표 스냅샷을 먼저 확정한 뒤 나머지를 삭제한다.

### 5.3 기존 PPT 데이터 초기 반영

- 현재 PPT에 포함된 과거 주간·월간 요약 수치는 초기 히스토리 데이터로 등록한다.
- 초기 히스토리에는 PPT에 포함된 모든 주간·월간 요약 수치를 입력한다.
- PPT에 없는 과거 Excel 원본 행 단위 상세 분석은 복원하지 않는다.
- 웹사이트 오픈 이후 업로드되는 Excel부터 상세 스냅샷을 축적한다.

## 6. Excel-PPT 지표 매핑

| 웹/PPT 지표 | Excel 원본 필드 또는 계산 기준 |
|---|---|
| Lead Status | `Lead Status` |
| Lead Source | `Lead Source` |
| 조직별 현황 | `Organization Level 2`, `Organization Level 3` |
| 담당자별 전환율 | `Lead Owner: Full Name` + `Lead Status` |
| Lead 생성 추이 | `Created Date` |
| Converted 건수 | `Lead Status = Converted` |
| Pipeline 상태 | `Converted Opportunity: Opportunity Status` |
| Pipeline 매출 | `Converted Opportunity: ORS Revenue` |
| GI | `Converted Opportunity: Total GI` |
| Business | `Main Interest Area`를 Business 매핑표로 변환 |
| Disqualified 분석 | `Disqualified Reasons` |
| 고객/니즈 보조 분석 | `Needs`, `Customer's Comments`, `Remarks` 등 |

## 7. 주요 웹 화면

- `Overview`: 최신 핵심 지표와 전주 대비
- `Weekly`: 주차별 Lead·상태·전환율
- `Monthly`: 월별 유입·상태·Business별 실적
- `Pipeline`: Opportunity·GI·매출·Won/Lost/On-going
- `Organization`: 전체 공개 조직별 Summary와 권한 기반 상세 Drill-down
- `Source & Product`: Lead Source와 상품군/Business 분석
- `History / Upload`: 업로드 이력, 기준일, 과거 스냅샷 관리

## 8. 보안 및 운영

- 사내 사용자만 접근 가능한 비공개 서비스로 운영한다.
- 인증 방식은 별도 초대 사용자 계정 방식으로 운영한다.
- 로그인 ID는 초대받은 사용자의 이메일 주소를 사용한다.
- 초대 사용자는 최초 로그인 시 자신의 비밀번호를 설정한다.
- 비밀번호를 잊은 사용자는 이메일 재설정 링크를 통해 비밀번호를 변경할 수 있다.
- 사용자는 초대된 계정으로 로그인해야 한다.
- 조직 단위 권한을 `Organization Level 2` 기준으로 적용한다.
- 권한 판정은 사용자의 허용된 `Organization Level 2`와 데이터의 `Organization Level 2`가 일치하는지를 기준으로 한다.
- 조직 권한 그룹명은 Excel의 `Organization Level 2` 값을 그대로 사용한다.
- `Organization Level 1` 또는 `Organization Level 3`의 상위·하위 계층 상속을 기본 권한으로 사용하지 않는다.
- `Overview`의 전체 요약 수치는 모든 로그인 사용자가 볼 수 있다.
- 조직별 `Summary` 집계 수치는 모든 로그인 사용자가 볼 수 있다.
- 상세 조회 화면의 부서별 집계 수치는 모든 로그인 사용자가 볼 수 있다.
- 부서별 집계 수치를 클릭해 Excel 원본 Lead Data의 행 단위 데이터로 이동하는 Drill-down은 해당 `Organization Level 2` 권한이 있는 사용자만 사용할 수 있다.
- 타 조직 사용자는 고객명 등 일부 필드만 가려진 형태를 포함해 타 조직의 원본 Lead Data 행 자체에 접근할 수 없다.
- 타 조직의 원본 Lead Data를 반환하는 API·다운로드·검색·상세 URL에도 접근할 수 없어야 한다.
- 상세 데이터는 로그인한 사용자의 `Organization Level 2` 권한을 서버에서 확인한 뒤 허용된 조직의 원본 행만 반환한다.
- 권한이 없는 조직의 상세 행·고객명·회사명·담당자명·Salesforce 코드 등은 조회할 수 없어야 한다.
- 화면에서 조직을 숨기는 것만으로 권한을 처리하지 않고, API/서버 조회 단계에서 접근을 차단한다.
- 고객명, 회사명, 담당자명, Salesforce 코드 등 원본의 민감할 수 있는 정보를 외부에 공개하지 않는다.
- 업로드 파일과 스냅샷은 웹 레포트의 데이터 저장소에서 관리한다.

### 8.1 권한 기본 역할

- 시스템 관리자는 사용자 초대, 계정 비활성화, 조직 권한 부여, Business 매핑 관리, Excel 업로드를 수행할 수 있다.
- 시스템 관리자는 모든 `Organization Level 2`의 요약 및 상세 고객 데이터를 조회할 수 있다.
- 일반 사용자는 기본적으로 하나의 `Organization Level 2`를 부여받고 해당 조직의 요약·상세 리포트만 조회할 수 있다.
- 본부장·부문장은 예외적으로 여러 개의 `Organization Level 2`를 권한 목록으로 부여받을 수 있다.
- 관리자는 사용자 관리 화면에서 사용자의 본부장·부문장 여부와 복수 `Organization Level 2` 권한을 직접 지정한다.
- 여러 조직 권한을 가진 사용자는 권한이 부여된 조직을 합산해 조회하거나 조직별로 전환해 조회할 수 있다.
- 조직 권한이 없는 사용자는 해당 조직의 상세 화면과 원본 행을 조회할 수 없다.
- 권한 변경 이후에는 다음 조회부터 변경된 권한을 적용한다.

## 9. 확인된 원본 수치

PPT의 대표 스냅샷 수치:

- Lead 합계: 380건
- Open: 50건
- Contacted: 27건
- Qualified: 7건
- Disqualified: 146건
- Converted: 150건
- Lead to Converted 전환율: 약 39.5%
- Pipeline 총매출: 421,625,671

현재 Excel 전체 데이터는 PPT와 범위와 기준일이 다르므로, 웹에서는 Excel 업로드 스냅샷별 결과를 기준으로 최신 수치를 표시한다.

## 10. 미확정 및 후속 인터뷰 항목

- 시스템 관리자 계정의 수와 관리자 권한 운영 방식
- 일반 사용자·본부장·부문장별 기본 화면 범위
- 초대 이메일의 발송 문구 및 유효기간
- 배포 환경과 접근 URL

## 11. 변경 이력

### 2026-09-11

- PPT 중심 웹 레포트에서 Excel 최신 데이터 자동 갱신 방식으로 결정
- 주간 관리와 함께 월간 관리 추가
- 주차를 월요일~일요일로 확정
- 기간 기준을 `Created Date`로 확정
- 상태 기준을 업로드 시점의 현재 `Lead Status`로 확정
- 전체 Excel 업로드 및 스냅샷 보존 방식으로 결정
- 기존 PPT 요약값을 초기 히스토리로 반영하기로 결정
- PPT에 포함된 모든 주간·월간 요약 수치를 초기 히스토리로 입력
- Business 매핑 기준 확정: DT / GC / BS / ETC
- 사내 전용 비공개 웹앱으로 결정
- 인증 방식은 별도 초대 사용자 계정으로 결정
- 조직별 상세 데이터에 조직 단위 접근 권한 적용
- 부서별 집계 수치는 전체 공개하고 원본 Lead Data Drill-down만 조직 권한으로 제한
- 조직 권한 기준을 `Organization Level 2`로 확정
- `Organization Level 2` 기준으로 서버/API 접근 제어
- Excel의 `Organization Level 2` 값을 조직 권한 그룹으로 그대로 사용
- 일반 사용자는 기본 1개 조직, 본부장·부문장은 복수 조직 권한을 가질 수 있도록 결정
- 필수 컬럼 15개 확정
- Excel 원본 컬럼명과 DB 컬럼명을 분리하고 Column Mapping으로 관리
- DB 컬럼명은 영문 `snake_case`를 기본안으로 채택
- DB 컬럼 매핑표는 컬럼별 사용자 확인 후 작성
- Overview 및 조직별 Summary 집계 수치는 전체 로그인 사용자에게 공개
- 고객명 등 Excel 원본 Lead Data Drill-down은 권한 사용자에게만 공개
- 상세 화면의 부서별 집계 수치는 전체 로그인 사용자에게 공개
- 타 조직 사용자는 타 조직의 원본 Lead Data 행·API·다운로드·검색에 접근 불가

### 2026-09-17

- 타 조직의 부서별 집계 수치는 공개하되, 타 조직의 원본 Lead Data 행 자체에는 접근할 수 없도록 권한 범위를 명확화
- 타 조직 원본 데이터의 API·검색·다운로드·상세 URL 접근도 차단
- 시스템 관리자는 모든 `Organization Level 2`의 상세 데이터까지 조회 가능
- 관리자 사용자 관리 화면에서 본부장·부문장 역할과 복수 조직 권한을 지정
- 이메일 주소를 로그인 ID로 사용하고 최초 로그인 시 사용자가 비밀번호를 설정
- 비밀번호 분실 시 이메일 재설정 링크로 비밀번호 변경
- `SFDC Lead Code`가 없는 마지막 Salesforce 내보내기 푸터 행은 제외
- 리포트 기준일을 사용자가 선택하고 해당 기준일의 과거 업로드 스냅샷을 표시
- 기준일 이후 데이터는 과거 기준일 리포트에서 제외
- 기준일 선택은 실제 업로드 스냅샷이 존재하는 날짜로 제한
- 임의 날짜 입력 및 인접 날짜 자동 대체는 사용하지 않음
- 동일 날짜 복수 업로드 시 마지막 업로드만 유효 스냅샷으로 유지
- 스냅샷 기준일은 Excel 업로드 날짜를 Asia/Seoul 기준으로 자동 기록
- 2년 초과 스냅샷은 월별 가장 빠른 날짜의 스냅샷 1개만 보관하도록 결정
- 최근 2년 이내 스냅샷은 날짜별 보관
- 2년 초과 스냅샷은 월별 가장 빠른 날짜의 1개만 보관
- 필수 컬럼 누락 또는 컬럼명 변경 시 업로드 거부 및 오류 안내
- DB 컬럼 매핑표 전체 확인 완료
- `Main Interest Area`에서 `Business` 컬럼을 파생하도록 결정
- 미매핑 상품명 발견 시 업로드 반영 중단 및 관리자 매핑 등록 필요
- 검증 통과 후 관리자 미리보기 제공
- 관리자 `반영` 버튼 클릭 시에만 운영 데이터 갱신

### 2026-09-17 — 웹 리포트 MVP 구현 상태

- 승인된 PPT 수치를 중심으로 Overview, 주차별 관리, 월도별 관리, Business/제품 화면의 정적 MVP를 구현했다.
- 기준 스냅샷 선택 UI, 조직 Summary 전체 공개, 권한 없는 조직의 Lead 상세 접근 차단 안내를 반영했다.
- 실제 Excel 업로드·DB 저장·초대 계정·서버 권한 검사는 다음 구현 단계에서 연결한다. 현재 화면의 스냅샷/조직별 수치는 PPT 기반 예시 데이터다.
- Sites 등록은 작업공간에서 비활성화되어 실패했다. 현재는 GitHub에 올릴 수 있는 독립 실행형 정적 웹 리포트로 진행하며, Sites 활성화 후 동일 소스를 배포 경로에 연결한다.
- Overview 첫 화면의 핵심 지표는 PPT `Lead 구간별 전환율 분석` 왼쪽 패널을 기준으로 `전체 Lead`, `Activity`, `Qualified`, `Converted`, `전환율`을 표시한다. 여기서 Activity는 `Lead Status`가 `Open`이 아닌 모든 Lead로 정의하고, 퍼널 지표 Qualified는 `현재 Qualified + Converted`로 정의한다. 따라서 대표 스냅샷에서 Activity는 330건, Qualified는 7 + 150 = 157건이다.
- Overview의 전주 대비 변화는 KPI 카드에 건수 증감과 증감률로 표시하고, 전환율은 퍼센트포인트(`%p`)로 표시한다. 세로형 퍼널 각 단계에는 전주 대비 증감 건수를 작게 병기하고, 단계 사이에는 이전 단계 대비 전환율을 표시한다.
